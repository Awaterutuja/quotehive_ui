import './filterBar.css';
const FilterBar = () =>{
        return(
            <div className="filterBarWrapper">
                <spna> hello</spna>
            </div>
        );
}

export default FilterBar;